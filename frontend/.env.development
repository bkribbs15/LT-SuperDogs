# Loaded automatically by `npm run dev` (vite). Points the app at the DEV
# backend on :8101 — the Masters pool uses :8000/:8001 on the same Mac mini.
# Prod builds bake VITE_API_URL via Dockerfile.prod.
VITE_API_URL=http://localhost:8101
